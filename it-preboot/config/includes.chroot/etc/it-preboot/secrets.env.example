# Copy this to /etc/it-preboot/secrets.env before building ONLY for lab use.
# Production recommendation: inject/mount secrets at boot rather than baking
# them into the ISO.
AZURE_SAS_TOKEN=""
VPN_USERNAME=""
VPN_PASSWORD=""
VPN_IPSEC_PSK=""
